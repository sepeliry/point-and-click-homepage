const GAME_CONDITIONS = {
  hasCompletedGame: "the player has completed the game",
  //hasCompletedGame: true,
  hasUnlockedDoor: "the player has unlocked the door",
  // hasUnlockedDoor: true,
};

export default GAME_CONDITIONS;
