{
  "name": "sepeli-point-and-click-homepage",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "@pixi/filter-glow": "^5.2.1",
    "marked": "^12.0.0",
    "pixi.js": "^7.3.3"
  },
  "type": "module",
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "npm run clean && parcel index.html --dist-dir deployment",
    "clean": "rimraf ./deployment ./.parcel-cache",
    "build": "npm run clean && parcel build ./index.html --dist-dir deployment",
    "build_serve": "npm run build && http-server ./deployment",
    "test": "mocha",
    "coverage": "c8 mocha"
  },
  "devDependencies": {
    "c8": "^9.1.0",
    "chai": "^5.1.0",
    "coveralls": "^3.1.1",
    "http-server": "^14.1.1",
    "mocha": "^10.3.0",
    "parcel": "^2.11.0",
    "parcel-reporter-static-files-copy": "^1.5.3",
    "rimraf": "^5.0.5"
  }
}
